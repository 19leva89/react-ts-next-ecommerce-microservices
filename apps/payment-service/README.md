```
npm install
npm run dev
```

```
open http://localhost:8002
```

# To run the ngrok tunnel:
terminal CommandPrompt -> `ngrok config add-authtoken <NGROK_AUTHTOKEN>`
terminal CommandPrompt -> `ngrok http --url=<NGROK_DOMAIN> 8002`